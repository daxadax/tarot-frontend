#spread{ data: { available_symbols: available_symbols, cards: cards.map {|c| c.id} }}
  #info
    = render :haml, :overlay, 
      locals: { cards: cards, 
                moon: moon,
                planetary_influence: planetary_influence}
  #unseen-cards
    = display_card_back_design
